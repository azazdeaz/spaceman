import React, {PropTypes} from 'react'
import {List} from 'react-matterkit'

export default class DialogComponent extends React.Component {
  static propTypes = {
  }

  static defaultProps = {
  }

  constructor(props) {
    super(props)
  }

  render() {
    return <div hidden/>
  }
}
