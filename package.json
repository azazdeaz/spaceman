{
  "name": "spaceman",
  "version": "0.0.0",
  "description": "react workspace - layout - manager",
  "main": "lib/nidex.js",
  "scripts": {
    "dev": "babel src --out-dir lib --watch",
    "prebuild": "rm -rf lib && rm -rf browser && mkdir browser",
    "build:lib": "babel src --out-dir lib",
    "build:browser": "browserify lib/index.js --standalone spaceman --debug | exorcist browser/spaceman.js.map > browser/spaceman.js",
    "build": "npm run build:lib && npm run build:browser",
    "prepublish": "npm run build"
  },
  "keywords": [
    "layout",
    "manager",
    "workspace",
    "react"
  ],
  "author": "Polgár András",
  "license": "MIT",
  "dependencies": {
    "lodash.has": "^3.0.0",
    "lodash.isarray": "^3.0.1",
    "lodash.merge": "^3.0.3"
  },
  "devDependencies": {
    "babel": "^4.7.13"
  }
}
