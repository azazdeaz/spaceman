{
  "name": "demo",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run prebuild && watchify src/index.jsx -t babelify --debug -o build/index.js",
    "prebuild": "rm -rf build && copyfiles -f src/index.html build",
    "build": "browserify src/index.jsx -t babelify --debug | exorcist build/index.js.map > build/index.js",
    "serve": "browser-sync start --server build --files 'build/**'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^9.0.3",
    "copyfiles": "^0.1.0",
    "watchify": "^2.4.0",
    "babelify": "^5.0.4",
    "browser-sync": "^2.3.1"
  }
}
